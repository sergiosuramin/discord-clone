export enum EServerExplorerType {
  Channel = 'channel',
  Member = 'member',
}

export enum EChatHeaderType {
  Channel = 'channel',
  DirectMessage = 'chat',
}

export enum ELockedChannelName {
  general = 'general',
}
